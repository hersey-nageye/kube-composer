# Cluster Issuer
# Purpose: To define a ClusterIssuer resource to manage the request, storage
# and renewal of TLS/HTTPS certificates within a Kubernetes cluster.

apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: "https://acme-v02.api.letsencrypt.org/directory"
    email: "hersey_nageye1@hotmail.com"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z1008549OXM7QL0PFM8S
          region: eu-west-2
      selector:
        dnsZones:
        - "eks.drhersey.org"
    - http01:
        ingress:
          class: nginx